{{/*
Same notation as .Values.config, rendered into a Secret instead, so the values
never appear in the pod spec.
*/}}
{{- if .Values.secret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ printf "%s-env" (include "harbor-scanner-trivy.fullname" .) }}
  namespace: {{ include "harbor-scanner-trivy.namespace" . }}
  labels:
    {{- include "harbor-scanner-trivy.labels" . | nindent 4 }}
  {{- with (include "harbor-scanner-trivy.annotations" (dict "root" . "local" dict)) }}
  annotations:
    {{- . | trim | nindent 4 }}
  {{- end }}
type: Opaque
data:
  {{- include "harbor-scanner-trivy.toEnvVars" (dict "values" .Values.secret "prefix" "" "isSecret" true) | nindent 2 }}
{{- end }}
