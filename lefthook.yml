---
pre-commit:
  parallel: true
  commands:
    spellcheck:
      glob: "*.{md,txt,yml,yaml}"
      run: typos --config .typos.toml {staged_files}

commit-msg:
  commands:
    conventional-commit:
      run: |
        msg=$(head -1 {1})
        pattern="^(feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert|upstream)(\(.+\))?(!)?: .+"
        if ! echo "$msg" | grep -Eq "$pattern"; then
          echo "ERROR: Commit message must follow Conventional Commits format"
          echo "  Format: <type>[optional scope]: <description>"
          echo "  Types: feat, fix, docs, style, refactor, perf, test, build, ci, chore, revert, upstream"
          echo "  Example: feat: add SBOM scan capability"
          echo "  Example: fix(queue): resolve retry backoff timing"
          echo "  Got: $msg"
          exit 1
        fi
    dco-signoff:
      run: |
        # Same regex as `task dco-check` and the DCO workflow. A plain grep for
        # the trailer key accepts a bare "Signed-off-by:" that both of those
        # reject, so the local hook has to require a name and an address too.
        if ! grep -qE "^Signed-off-by: .+ <[^<>@[:space:]]+@[^<>[:space:]]+>[[:space:]]*$" {1}; then
          echo "ERROR: Missing DCO sign-off. Use 'git commit -s'"
          exit 1
        fi
